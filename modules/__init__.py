"""Pillar modules. Each implements an orchestrator interface and imports only
from `shared/` and `orchestrator/interfaces/`, never from a sibling module
(coding-practices.md section 2, enforced by scripts/check_module_imports.py).
"""
