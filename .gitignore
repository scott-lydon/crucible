.DS_Store
Thumbs.db
node_modules/
*.log
.env
.env.*
# Keep the checked-in template even though .env.* is ignored.
!.env.example

# Raw datasets (large, license-bound). Trained model artifacts under artifacts/
# ARE committed; the raw creditcard.csv is not.
data/

# Python
.venv/
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Local scratch: throwaway debug scripts and one-off target outputs that land
# at the repo root during demo runs. Real targets live under modules/targets/.
scratchpad/
/fraud_predict.py
/sum_two_integers.py
/verify_asserts.py

# Intermediate video-stitching artifacts produced while building the demo reel.
demo/_stitch_tmp/

# Throwaway integrity capture (superseded by the canonical local-us6-catalog.*).
demo/integrity/us6-LOCALTEST.*
