Revise this Live Run View, keeping the established palette, layout, and all three states (happy, empty/connecting, error/halted). Apply these specific fixes and nothing else ; 1. Halt state: reconcile the red-line crossing and the halt round. Add an inline note at the halt marker reading "crossed r.32 · confirmed over 3-round debounce window · halted r.35" so the lag is a documented policy, not an apparent control failure ; 2. Ledger projection: change "projected at r.48 $8.74" to show its basis, e.g. "projected at r.48 $8.74 (avg/round × remaining)", render it as a small range, and make it look clickable to its calc the same way the hard stop links to MRG-12.4 ; 3. Cost meter SESSION: add a denominator and window so it is not a bare number, e.g. "SESSION $65.58 · 7 runs · since 09:14Z" ; 4. Add the run date once in the run-summary strip, e.g. "r_8f3a · 2026-06-22", so every HH:MM:SSZ timestamp resolves to a specific day ; 5. Remove the pulsing expanding box-shadow ring animation on live dots and the shimmer animation on skeletons; keep static green/amber dots plus the text label and replace the shimmer skeleton with a static muted bar reading "awaiting verdict" ; 6. Judge one-vote weight: drop the "½" glyph everywhere and use the explicit token "1/5" (or "1 of 5") identically in the streaming, empty, and halted states so the LLM judge unambiguously carries one of five signals ; 7. Top-right cost meter: give THIS RUN visual primacy with a thin inline progress bar under "$4.18 / $25.00" and a color threshold that turns amber past ~70% and danger past ~90% of the ceiling, so it warns before the hard stop ; 8. Reconnecting state: keep the SESSION column in place but render it greyed and stale with a "last known" caption instead of swapping a different metric under a new header ; 9. ASR vs Detection chart: add a per-series directional cue ("ASR ↓ better", "Detection ↑ better") and shade the danger zone (ASR above and Detection below the 0.90 red line) so the worsening direction is obvious without reading the legend ; 10. Accessibility contrast: lighten all data-bearing muted text from #95A1AE to at least #B8C2CE (about 7:1) and raise minimum data mono text from 10px to 12px ; 11. Raise the "REVIEW ONLY" divider text to at least #8A94A2 ; 12. Reconnect card values: use #F0DA6A or #FFE9A8 on the brown #221C0C panel so amber values clear 4.5:1 contrast ; 13. Every colored status dot must carry an adjacent plain-language state word ("ok", "slow", "degraded", "connected", "dropped"), never color alone ; 14. Verdict stream: add a persistent visible header row in plain language (Round, Time, Oracle, Verdict, Cost) above the chips, and give the active All/Fails/Evasions filter an underline or "selected" label rather than background fill alone ; 15. Add a small text-labeled "about" affordance (a focusable, clickable "i" with a visible label, not hover-only) next to each headline metric (ASR, Detection, pass_rate, recall, drift) that would open its definition, unit, and data source ; 16. Raise all SVG chart annotation text (axis labels, "red line · recall >= 0.90", "r.32 · crossed", "backfill pending") to at least 11px at 4.5:1 contrast, and restate the red-line threshold and crossing round in adjacent body text outside the SVG.
