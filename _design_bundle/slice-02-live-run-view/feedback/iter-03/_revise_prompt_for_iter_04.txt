Revise slice-02 live-run-view. Apply ALL of these fixes, keep everything else identical, do not regress the iter-03 fixes (parent-doc MRG-12 labeling, session 'context only' demotion, 'about' details popovers, visible projection/threshold text, disabled-label contrast): (1) State 3 exit-criteria contradiction: the run-summary Resume control says 'blocked · needs risk-approver override (recall red line · MRG-12.6)' but the halt-rationale drawer closes with 'The blue loop, not an override, is the path back to certification.' Make both surfaces state one model identically. If remediation is the only path back, change the Resume caption to 'blocked · clears only when white-box recall recovers to 0.90 on a held-out attack set (no override) · MRG-12.6'. ; (2) State 3 crossing-round reconciliation: the chart marker says 'crossed r.32' but the only observed recall shown is 'OBSERVED · r.35 = 0.71', and the per-round held-out chips show 0.94 at r.33 and don't fail until 0.86 at r.34, so an examiner cannot see a sub-0.90 reading at r.32. Add an 'OBSERVED · r.32' tile showing the white-box recall value that first dropped below 0.90 at the crossing round, so the crossing round and the binding sub-0.90 reading are the same verifiable number; keep r.35 = 0.71 as the terminal value. ; (3) State 2 reconnect counters: the header pill 'SSE reconnecting · attempt 3' and RECONNECT card 'attempt 3 of 6' disagree with the TRANSPORT card 'reconnects 1' on the same screen. Disambiguate: label the retry field 'retry attempt 3 of 6 (this drop)' and the transport field 'reconnects (this run) 1' so the two distinct counters can't read as a contradiction. ; (4) Mirror the iter-03 budget-bar threshold fix into States 2 and 3: State 3 'THIS RUN · FROZEN' bar carries its 'cyan <70% · amber 70-90% · danger >90%' legend only in a title attribute while State 1 shows it as visible text. Restate the thresholds as visible text in State 3 (and add the same visible legend to the State 2 stale bar, or drop the color encoding when frozen), and remove the title attribute as the sole carrier. ; (5) The new 'about' details popovers use role='dialog' with no visible close control and no Escape cue, unlike the inspect/replay/halt drawers which have an explicit close button. Either drop role='dialog' and treat each as an inline disclosure (aria-expanded on the summary), or add a text-labelled 'close' control inside the popover and a visible Escape-to-dismiss hint. ; (6) The verdict-stream column header row (Round / Time / Oracle / Verdict / Cost) appears only in State 1; State 2 (buffered) and State 3 (halted) render the same cards with no header row. Add the same labeled header row above the verdict cards in States 2 and 3, or promote the verdict stream to a semantic table with persistent header cells across all three states.
