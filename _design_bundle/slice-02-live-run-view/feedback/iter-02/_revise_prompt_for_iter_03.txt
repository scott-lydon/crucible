Revise slice-02 live-run-view. Apply ALL of these specific fixes, keep everything else identical, do not regress prior fixes: (1) Control-policy citations: cost-ledger 'hard stop $25.00 · MRG-12.4' (state 1 ledger) vs halt-certification 'MRG-12.6' cited in the halt banner, halt rationale card, audit shortcut, and halt drawer: Label each citation with what it governs so the relationship is explicit, e.g. 'spend ceiling · MRG-12.4' and 'recall red line · MRG-12.6', and ideally show the parent policy once (e.g. 'Model Risk Governance MRG-12') so both clauses resolve to one document. ; (2) Halt drawer RULE block (state 3 halt rationale): 'if recall_white_box < 0.90 then halt_certification() ... until recall_held_out >= 0.90': Make the gate metric and the resume metric the same series, or state explicitly why they differ and which is the binding control (e.g. 'halt on white_box < 0.90; resume requires white_box >= 0.90 confirmed on a held-out attack set'), so the entry and exit thresholds are the identical measurement on the identical population. ; (3) State 3 halt round 35: aggregate label 'FAIL · 3 of 4 oracles flipped' alongside the four oracle cards (Held-Out FAIL, Metamorphic FAIL, Differential FAIL, Property Fuzz PASS) plus the separate 'LLM Judge ... weight 1/5': State the exact aggregation arithmetic at the halt, e.g. '3 of 4 deterministic oracles FAIL; LLM judge PASS at weight 1/5; weighted result FAIL' so the count in the header, the cards, and the judge weight all reconcile to one number an examiner can recompute. ; (4) State 3 chart 'Detection 0.71' plotted against the 'recall >= 0.90' red line, while the halt banner and rationale attribute the stop to 'white-box self-test recall 0.71': Make the gating series unambiguous: either rename the chart's cyan series to 'White-box recall' on the halted state so it matches the banner/rationale, or add a one-line caption under the chart stating 'Detection (live) and white-box recall (gating) are the same series' / 'are distinct — only white-box recall gates the halt'. ; (5) State 3 halting-round judge footer reads '✓ PASS · weight 1/5' while the streaming and round-22/21 rows use a bare '1/5' badge: Drop 'weight' from the halting-round footer; use the same bare '1/5' (or '1 of 5') badge token used in every other round row so the judge weight reads identically in all states. ; (6) State 3 halt-rationale RULE block: trigger is 'recall_white_box < 0.90' but the until-clause is 'recall_held_out >= 0.90' (different variable name): Either align the names (use recall_white_box for both, or recall_held_out for both) or add one comment line in the rule block, e.g. '// held-out, not white-box, prevents re-overfitting to the known scheme', so the deliberate asymmetry is legible rather than reading as a typo. ; (7) Cost meter SESSION column: state 1 shows '$65.58 · 7 runs · since 09:14Z'; THIS RUN ceiling is $25.00: Either add a session ceiling/cap next to the $65.58 (e.g. '/ $200.00 org budget') or visually demote SESSION to a muted, caption-weight figure so it reads as informational-only and only THIS RUN reads as a gated meter. ; (8) New 'About' metric affordances next to ASR 0.18 and Detection 0.94: circular buttons whose only label is a bare lowercase 'i' glyph, with the definition/unit/source carried entirely in a title= tooltip: Replace the title= tooltip with a click/focus-activated popover or inline expandable definition (focusable, opens on click and on Enter/Space), and pair the glyph with a short visible word ('about') rather than a bare 'i'. The definition, unit, and data source must be reachable without hover. ; (9) Cost-projection control in the live ledger ('$8.20 – $9.30 / avg/round × remaining ↗') and the 'THIS RUN' / ceiling progress bars, whose calculation method and amber/danger thresholds live only in title= attributes ('avg $/round × rounds remaining + 95% confidence band · click for calc' and 'cyan <70% · amber 70–90% · danger >90%'): Surface the projection method and the confidence band as visible body text under the figure, and restate the budget-bar thresholds ('warns at 70%, danger at 90%') as visible text near the bar rather than only in title=. Do not let a color band be the sole carrier of the threshold state. ; (10) Disabled control affordance on the reconnecting state: 'Pause · waiting' / 'Halt run · waiting' buttons rendered in #5A6573 text on #1A222B: Raise disabled-button label text to at least #8A94A2 (≈4.5:1 on #1A222B) and keep the explanatory 'waiting' / disabled reason as visible text, not only in the title= attribute.