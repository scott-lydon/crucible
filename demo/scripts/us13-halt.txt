This is halt certification. The platform stops issuing new clean verdicts automatically when verifier recall drops below the configured red line. Right now observed recall is one hundred percent, at or above the point seven red line, so the residual halt threshold is clear and no runs are blocked. There is no operator halt button. The state is automatic, and the page honestly notes the backend exposes only the current halt state, with no halt history to invent.
