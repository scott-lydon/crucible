User story one: submit a target for evaluation. On the Run Launcher I select the Code Agent target, an autonomous code generation agent. The check mark confirms the target is now selected. I seal the specification, which locks the obligations and invariants the target must satisfy. Then I click Run evaluation. Crucible accepts the sealed spec, launches the producer sandbox, and navigates to the running view, where the first attack round begins within seconds against the live model.
