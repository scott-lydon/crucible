This is the internal admin and debug route, backed entirely by real endpoints. The overrides log is empty. No operator overrides have been recorded, and that is shown honestly rather than faked with placeholder rows. It reports the current identity as anonymous, the workspace as default, and the operative policy in plain language: certification halts when white box recall falls below the point seven red line.
