Drilling into a single verdict on the results tab. The producer broke this obligation, and the verdict is sealed. Two oracles passed and zero blocked, for a pass weight of one point five out of four point five votes, below the two vote threshold needed to pass. Each oracle shows its basis, obligation o one, and its own weight. And the replay verdict button re-runs the exact captured decision deterministically.
