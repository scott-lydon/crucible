The seeded hack corpus is exported so the benchmark outlives the demo. The export link downloads it as JSON lines, one line per successful attack, each carrying the attack id, the target type, the tactic, the exact prompt, the full audit trace, the dollars it cost, and when it was captured. The corpus currently holds one successful evasion, and the exported file's row count matches it exactly.
