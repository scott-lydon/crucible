The sealed attack corpus is exported so the benchmark outlives the demo. This catalog lists ninety-five attack tactics, and the export JSON link downloads the full corpus as JSON lines, one line per successful attack, each carrying the attack id, the target type, the tactic, the exact prompt, the full audit trace, the dollars it cost, and when it was captured. The corpus file holds ninety-one successful evasions, and its row count matches the corpus endpoint exactly.
